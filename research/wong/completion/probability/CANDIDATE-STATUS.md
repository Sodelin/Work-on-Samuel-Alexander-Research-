# Probability candidate: pinned hosted verification pending

This packet proposes **19 selected endpoints**, taking the real-development
inventory from the previously verified **274 to 293**. Source correspondence
and an independent statement review passed with scoped limits. **The combined
293-endpoint compiler audit has not passed yet.** Admission requires the
existing strict hosted CI on the exact published candidate commit.

The mathematical result concerns a stopped lineage-count path law and
almost-sure finite jump-index absorption. Calendar time, a marked spatial ARG,
Big/Little projection, and exact expected-event asymptotics remain open.
Appendix B coverage rows B04 and B05 remain partial.

## Evidence with its actual scope

| Evidence | What it establishes | What it does not establish |
|---|---|---|
| Historical worker logs | Three source-matched local exit-zero runs, 19 selected reports and permitted standard axiom sets | An independent source-pin binding for the donor dependency cache |
| Source and adversarial reviews | Literal-rate correspondence, an actual path law, stated assumptions and bounded interpretation | A fresh compiler run or hosted integration |
| Lead's local integration attempt | The scalar module passed; all 61 prior local source/object bindings were checked against the 274 receipt | The count module, bridge and 293 aggregate did not complete |
| Hosted deterministic baseline | The earlier 274 release passed at the exact commit linked below | The additional 19 probability endpoints |

The historical worker used `J:/CodexTools/extinction-dependency-cache` in
addition to the pinned source checkout. A later read-only check found the
required dependency objects, but no independent acquisition record binding
that donor cache to the pinned Mathlib source. Existence and compatibility are
not authentication. The historical logs and review snapshot are retained
unchanged; their pin labels are not upgraded into stronger evidence.

The lead's bounded retry deliberately did not use that donor. Its scalar
module passed, then `WongCountChain` stopped while loading a missing
`Mathlib.Probability.Kernel.IonescuTulcea.Traj.olean` from the primary cache.
This was an import/setup failure before the count proofs could be checked.
The bridge and aggregate were not run. The exact [attempt record](local-attempt/ATTEMPT-STATUS.json),
[scalar log](local-attempt/logs/WongBigARGDrift.txt) and
[import failure](local-attempt/logs/WongCountChain.txt) are included.

## Admission gate

The publisher will use the repository's existing official source-keyed
Mathlib cache route at revision `0df444a360eaa60ab8c11dca51a86af692955474`
with Lean `4.33.1`, adding the four cache roots listed in
[REQUIRED-CACHE-IMPORTS.json](REQUIRED-CACHE-IMPORTS.json). The strict auditor
and its axiom policy stay unchanged. See [REPRODUCE.md](REPRODUCE.md) for
commands and the exact acceptance condition.

The existing root `verification/real-audit.json` remains the earlier 274
receipt. A preserved [274 receipt copy](evidence/baseline-real-audit-274.json)
is included here. There is no fabricated or inferred 293 receipt. The verified
baseline is [commit 46aac52](https://github.com/Sodelin/Work-on-Samuel-Alexander-Research-/commit/46aac52e214311fb2c2230b1b4fe37c42ef9e1a9),
[hosted run 36158710859](https://github.com/Sodelin/Work-on-Samuel-Alexander-Research-/actions/runs/36158710859).

The [result explanation](RESULT.md), [source comparison](SOURCE-CORRESPONDENCE.md),
[independent review](ADVERSARIAL-REVIEW.md), and their manifests are immutable
historical review artifacts. This status file supplies the later provenance
qualification. [CANDIDATE-STATUS.json](CANDIDATE-STATUS.json) is the structured
current candidate record. A successful hosted run should be recorded in a new
publication status record, preserving this candidate history.
